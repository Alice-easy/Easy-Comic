build:
  maxIssues: 10

config:
  validation: false

console-reports:
  active: true

output-reports:
  active: true

comments:
  active: false

complexity:
  active: true

coroutines:
  active: true

empty-blocks:
  active: true

exceptions:
  active: true

naming:
  active: true

performance:
  active: true

potential-bugs:
  active: true

style:
  active: true
